#!/usr/bin/env stormscript
lib system;

do{
    cmd: arg[1];

    if cmd is "out"{
        printl "Hello, World!";
    }else{
        printl "helloworld library doesn't have command '", cmd, "'.";
    }
}
