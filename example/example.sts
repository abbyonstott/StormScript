type person {
    str name;
    int age;

    func birthday {
        age +: 1;
    }

    init => nm, _age {
        name: nm;
        age: _age;
    }
}

person p => "bob", 30;

printl p.name, " is " p.age;

p.birthday;

printl p.name, " is " p.age;